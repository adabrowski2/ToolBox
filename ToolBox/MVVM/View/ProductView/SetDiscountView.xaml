<Window x:Class="Praca_Inżynierska_v1.MVVM.View.SetDiscountView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStartupLocation="CenterScreen"
        Title="Ustal rabat"
        Height="300" Width="500"
        Background="#EAF4FF"
        ResizeMode="NoResize"
        WindowStyle="None">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Ustal rabat dla produktu" FontSize="20" FontWeight="Bold" Foreground="#1F468F"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,20,0,10">
            <TextBlock Text="Rabat (%)" Width="100" VerticalAlignment="Center"/>
            <TextBox x:Name="DiscountTextBox" Width="100"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Ważny do" Width="100" VerticalAlignment="Center"/>
            <DatePicker x:Name="EndDatePicker" Width="150"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Anuluj" Width="100" Margin="0,0,10,0" Click="Cancel_Click"/>
            <Button Content="Zapisz" Width="100" Click="Save_Click" Background="#1F468F" Foreground="White"/>
        </StackPanel>
    </Grid>
</Window>